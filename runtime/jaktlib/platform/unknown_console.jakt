import jakt::utility { panic }
import ide::console_common { ReadResult }

fn read_from_stdin(buffer: raw c_char, buffer_size: usize) throws -> ReadResult {
    panic("Compiler with unknown platform cannot read from input. Please obtain a Stage 1 build of Jakt to use this feature.")
}
